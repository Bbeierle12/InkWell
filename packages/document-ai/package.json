{
  "name": "@inkwell/document-ai",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:unit": "vitest run",
    "test:fuzz": "vitest run --fuzz",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@inkwell/shared": "workspace:*",
    "eventsource-parser": "^3.0.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.8",
    "fast-check": "^3.23.2",
    "msw": "^2.7.0",
    "prosemirror-model": "^1.25.4",
    "prosemirror-test-builder": "^1.1.1",
    "vitest": "^2.1.8"
  }
}
