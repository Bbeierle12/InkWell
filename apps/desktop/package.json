{
  "name": "@inkwell/desktop",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "build:debug": "tauri build --debug",
    "build:local-inference": "tauri build -- --features local-inference",
    "typecheck": "cd src-tauri && cargo check",
    "test": "cd src-tauri && cargo test"
  }
}
