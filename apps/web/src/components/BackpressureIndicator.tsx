'use client';

/**
 * BackpressureIndicator Component
 *
 * Shows "suggestions paused" or "local mode" status when the system
 * is under backpressure or operating offline.
 */

export function BackpressureIndicator() {
  // TODO: implement
  // - Subscribe to backpressure state from DocumentAI
  // - Show indicator when paused or in local-only mode
  return null; // Hidden by default
}
